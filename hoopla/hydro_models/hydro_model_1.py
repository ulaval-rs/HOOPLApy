from typing import Dict

import numpy


class HydroModel1:

    def __init__(self):
        drftc = numpy.ceil()
        dl = numpy.zeros()


def _hydro_model_1(
        precipitation,
        pet,
        soil_reservoir_capacity,
        soil_reservoir_overflow_dissociation_constant_R,
        routing_reservoir_emptying_constant,
        delay,
        rainfall_partitioning_coefficient,
        routing_reservoir_emptying_constant_R_T,
        soil_reservoir_state,
        root_layer_reservoir_state,
        direct_routing_reservoir_state):
    """

    Parameters
    ----------
    precipitation
        Mean areal rainfall (mm)
    pet
        mean areal evapotranspiration (mm)

    Returns
    -------

    """

